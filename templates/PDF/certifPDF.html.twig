{% block body %}  
  
  <section class="main-banner" id="top">
    <div class="container">
      <div class="row">
        <div class="col-lg-6 align-self-center">
          <div class="header-text">
            
            <div class="right-image" style="text-align: center;">
          <img src= "{{ asset('images/' ~ Certif.image) }}" style="width: 500px; height: auto;" />
            {# <img src="assets/images/banner-right-image.png" alt=""> #}
          </div>
            {# <h6>{{Formation.dateFormation|date('Y-m-d')}}</h6> #}
            
            {# <p>Description : {{Certif.description}}</p> #}
            <p>Date Certification : {{Certif.dateCertif|date('Y-m-d')}}</p>
            <p>Formation : {{ Certif.idFormation.Libelle }}</p>
            {# {% for element in Certif.idFormation %}
                                           <p> {{ Certif.idFormation.Libelle }}</p>
                                        {% endfor %}
            <p>Auteur : {{Certif.auteur}}</p> #}
            
            
          </div>
        </div>
        <div class="col-lg-6">
          
        </div>
      </div>
    </div>
  </section>

  {% endblock %}