{% extends 'base_front.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}

<style>
    /* Define the primary color */
    :root {
        --primary-color: #007bff;
    }

    /* Use the primary color for headings and buttons */
    h3, .btn-primary {
        color: var(--primary-color);
    }

    /* Increase the font size of headings */
    h3 {
        font-size: 36px;
    }

    /* Add more whitespace between sections */
    .form-group {
        margin-bottom: 30px;
    }

    /* Add icons to form labels */
    .form-group label::before {
        content: '\f007';
        font-family: FontAwesome;
        font-size: 16px;
        color: var(--primary-color);
        margin-right: 10px;
    }

    /* Add hover effects to buttons */
    .btn-primary:hover {
        background-color: #0069d9;
        border-color: #0062cc;
    }
</style>




<section class="login-section">

    <div class="container d-flex flex-column justify-content-center align-items-center mt-700" style="height: 80vh;">
            <div class="row">
                <div class=" mx-auto">




  
 {# <div class="container" style="margin-top: 150px;"> #}
  {# <div class="container d-flex flex-column justify-content-center align-items-center mt-500" style="height: 80vh;"> #}


    <div class="card" style="align:center">
    {# <div class="card-body"> #}
    <div class="col-10 mx-auto" style="margin-top:100px">
    <div class="card-body text-center">
        <h1 class="text-center mb-4 mt-0 text-primary">Reset your password</h1>
   {# <div class="col-lg-12"> #}
          {# <div class="owl-service-item owl-carousel">                  #}
 
        <div class="form-group">
            {{ form_start(resetForm) }}</div>
        <div class="form-group">
            {{ form_row(resetForm.plainPassword) }}</div>
        <button class="btn btn-primary">Reset password</button>
        <div class="form-group">
            {{ form_end(resetForm) }}</div>

        </div>
        </div>
        </div>
        </div>
        </div>
        
        
{% endblock %}

{# <div class="card">
                <div class="card-body text-center">
                    <h1 class="text-center mb-4 mt-0 text-primary">Please sign in</h1>
                    <form method="post">
                        <div class="form-group">
                            <label for="inputEmail"></label>
                            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control form-control-lg"  placeholder="Entrez votre email"  autocomplete="email" required autofocus>
                        </div>
                        <div class="form-group">
                            <label for="inputPassword"></label>
                            
                        <input type="password" name="password" id="inputPassword" class="form-control form-control-lg " placeholder="Entrez votre password" autocomplete="current-password" required>
                        </div>
                        <div class="checkbox mb-3">
                    <label>
                     <input type="checkbox"name="_remember_me"> Remember me <a href="{{ path('app_forgot_password_request') }}">Forgot password?</a>
             </label>
                    </div>
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                        <div class="form-group">
                            <button class="btn btn-lg btn-primary btn-block" type="submit">Sign in</button>
                       
                    </form>
                
                    
                </div>
            </div> #}