 {% extends 'base_back.html.twig' %} 
{% block body %}
 <table>
                                    <tr>
                                    <th>  Id  </th>
                                    <th>  presence  </th>
                                    <th>  user </th>
                                    <th>  fromation </th>
                                    
                                    
                                    </tr>
                                    
                                    {# {% for i in inscriptions %} #}
                                    <tr>
                                        <td>{{i.id}}</td>
                                        
                                        
                                        {% if i.isPresent %}
                                        <td><a href="{{ path('inscription_update', {'id': i.getId()}) }}" >présent</a></td>
                                        {% else %}
                                        <td><a href="{{ path('inscription_update', {'id': i.getId()}) }}" >absent</a></td>
                                        {% endif %}
                                        <td>{{i.idPersonnel.nom}}</td>
                                        <td>{{i.idFormation.libelle}}</td>
                                        
                                    </tr>    
                                    {# {% endfor %} #}

                                </table> 
                                {% endblock %}