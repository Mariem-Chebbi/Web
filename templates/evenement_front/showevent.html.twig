{% extends 'base_front.html.twig' %}
{% block title %}Evenement{% endblock %}

{% block body %}
<section class="services" id="services">
    <div class="container">
      <div class="row">
        <div class="col-lg-12">
          <div class="section-heading">

               <div class="row  border-bottom white-bg dashboard-header">
 <div class="ibox-content">
 <div class="row">

    <h1>Evenement</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Nom_event</th>
                <td>{{ evenements.nomEvent }}</td>
            </tr>
            <tr>
                <th>Lieu_event</th>
                <td>{{ evenements.LieuEvent }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ evenements.Description }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ evenements.DateDebut ? evenements.DateDebut|date('Y-m-d ') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ evenements.DateFin ? evenements.DateFin|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>
  <div class="main-button-gradient">
    <a href="{{ path('frontevent') }}" >back to list</a><br></br>
</div>
    
    <br>

   
    </div>
    </div>
    </div></div></div></div>
   </section>
{% endblock %}